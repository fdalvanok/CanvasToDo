{
  "name": "canvas-todo",
  "version": "0.1.0",
  "description": "Fetch upcoming Canvas assignments, generate a study scaffold for each with the Anthropic SDK, and publish them into a Notion database.",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "@notionhq/client": "^2.2.15",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
